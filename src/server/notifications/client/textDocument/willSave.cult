(require "anon:boot--1")
(define willSave (function (context request) (do
  (define logMessage (map/get (map/get context "handlers") "window/logMessage"))
  (logMessage context {
    "params" {
      "type" 3
      "message" "File Will Save"
    }
  })
)
))