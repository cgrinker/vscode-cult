(require "anon:boot--1")
(define didOpen (function (context request) (do
  (define logMessage (map/get (map/get context "handlers") "window/logMessage"))
  (logMessage context {
    "params" {
      "type" 3
      "message" "File Did Open"
    }
  })
)
))